// The global variable
let fixedValue = 4;

// Only change code below this line
function incrementer(incremenValue) {
  return incremenValue+ 1;
  // Only change code above this line
}
incrementer(fixedValue)